{
	"scripts": {
		"predev": "npm run backup:daily",
		"backup:daily": "node scripts/daily-db-backup.js",
		"dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
		"build": "npm run build --prefix backend && npm run build --prefix frontend && node -e \"require('fs').rmSync('backend/dist/client',{recursive:true,force:true});require('fs').cpSync('frontend/dist','backend/dist/client',{recursive:true})\"",
		"start": "react-scripts start ",
		"test": "cross-env CI=true react-scripts test --env=jsdom",
		"eject": "react-scripts eject",
		"format": "biome format --write .",
		"check": "biome check --write .",
		"docker:up": "docker-compose up --build -d",
		"docker:down": "docker-compose down",
		"docker:logs": "docker-compose logs -f",
		"docker:backup": "docker run --name mongo-backup --env-file .env -d mongo-backup"
	},
	"devDependencies": {
		"@biomejs/biome": "2.3.10",
		"concurrently": "^9.2.1",
		"cross-env": "^10.1.0",
		"jiti": "^2.6.1"
	},
	"dependencies": {
		"npkill": "^0.12.2",
		"tesseract.js": "^7.0.0",
		"xlsx": "^0.18.5"
	}
}
